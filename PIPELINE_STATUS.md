# Pipeline Status

Operational handoff only. `LEADS.md` and `OUTREACH_LOG.md` remain the source of truth.

- Current phase: Build complete. Independent review complete (2026-07-14) — blocking fix applied and re-verified. Proceeding to deploy.
- Last trusted commit: 1d24bc8 "Build Brankley Farm Kennels & Cattery demo site (3 pages)" (initial commit) — CSS fix below not yet committed at time of writing.
- Fixed (2026-07-14): `css/style.css` CSS specificity bug — the homepage's final CTA section eyebrow ("Ready to book?") was rendering dark terracotta-on-dark-green (contrast ~1.4–2.0:1, failed WCAG AA 4.5:1) instead of white, because `.section-head .eyebrow` (line 380) tied in specificity with and was declared after `.page-hero ... .eyebrow` (line 337). Fixed by adding `.page-hero .section-head .eyebrow { color: var(--white); }` right after line 380 (specificity 0,3,0, unambiguously wins). Verified via `getComputedStyle` on a locally-served page (Playwright scratch install): CTA eyebrow now computes to `rgb(255, 253, 247)` (white), matching expectation exactly. Re-ran `.pipeline/qa/contrast-audit.js` on index.html at 390/834/1440px: still exactly 10 violations, same pre-existing documented false positive on the top hero-photo section (img+scrim the script can't resolve) — no new violations, no regression. services.html/contact.html unaffected (their page-hero eyebrows aren't nested inside `.section-head`). Full detail in QA_REPORT.md "Fix Verification" section.
- Next exact action: Deploy — create public GitHub repo `Plainset/brankley-farm-kennels-demo`, push, enable Pages, confirm live URL loads, then draft outreach email per AGENTS.md step 7.
- Deploy URL: pending (see final report)
- Outreach state: pending (see final report)
- Flags for Alex:
  - The business's own site's footer still cites "Daventry District Council" for its animal boarding licence — that authority was abolished in 2021 and merged into West Northamptonshire Council. Demo site copy uses "the local authority (West Northamptonshire Council)" instead of repeating the outdated council name. Independent reviewer re-examined this claim on 2026-07-14 and judged it safe to ship as worded: the Daventry DC→WNC transfer (incl. animal-licensing functions) is settled UK public administrative record, not a speculative claim, and the licence number itself is taken verbatim from the business's own site. See QA_REPORT.md Independent Reviewer Pass and BUILD_BRIEF.md Business State Check for full reasoning.
  - Pitch hook for outreach: brankleyfarm.co.uk fails to load over HTTPS entirely (SSL handshake failure on both bare and www domains) — real visitors on modern browsers will likely hit a security warning before ever reaching the working HTTP fallback. Confirmed again this session via curl: HTTPS fails, HTTP works and redirects to www.
